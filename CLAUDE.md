# CLAUDE.md

The working agreements for this repository are in [`AGENTS.md`](AGENTS.md).
They apply here in full.

Kept as a separate file because Claude Code loads `CLAUDE.md` automatically.
Everything that applies to any agent belongs in `AGENTS.md`, so the two
cannot drift apart.
